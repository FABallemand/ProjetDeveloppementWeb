{% extends 'base.html.twig' %}

{% block title %}Edit Shoe{% endblock %}

{% block main %}
    <h1>Edit Shoe</h1>

    {{ include('shoe/_form.html.twig', {'button_label': 'Update'}) }}

    {{ include('shoe/_delete_form.html.twig') }}

    <div class="p-4 pt-0 border-top-0 bg-transparent">
        <div class="text-center"><a class="btn btn-info mt-auto" href="{{ path('app_shoe_index') }}">Back to list</a></div>
    </div>
{% endblock %}
