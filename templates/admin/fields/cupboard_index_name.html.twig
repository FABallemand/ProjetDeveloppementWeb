{%- set url = ea_url()
    .setController('App\\Controller\\Admin\\CupboardCrudController')
    .setAction('detail')
    .setEntityId(entity.instance.id)
-%}
{% set action = app.request.attributes.get('_controller')[1] %}
{% if action == 'detail' %}
    {{ field.value }}
{% else %}
	<a href="{{ url }}">
		{% if entity.instance.shoes is empty %}
        <strike>{{ field.value }}</strike>
		{% else %}
            {{ field.value }}
        {% endif %}    
 	</a>
 {% endif %}