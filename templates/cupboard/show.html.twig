{% extends 'base.html.twig' %}

{% block title %}Cupboard {{ cupboard.id }}{% endblock %}

{% block body %}
<h1>Cupboard {{ cupboard.id }}</h1>

{% dump cupboard %}
{{ dump(cupboard) }}

<table class="table">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ cupboard.id }}</td>
        </tr>
        <tr>
            <th>Cupboard</th>
            <td>{{ cupboard.name }}</td>
        </tr>
        ...
    </tbody>
</table>

<a href="{{ path('cupboard_index') }}">back to list</a>

{% endblock %} {# body #}